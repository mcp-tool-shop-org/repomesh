name: registry-ci

on:
  push:
    branches: [main]
    paths:
      - 'ledger/nodes/**'
      - 'ledger/events/**'
      - 'registry/scripts/**'
      - '.github/workflows/registry-ci.yml'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build-registry:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Build registry index
        run: node registry/scripts/build-registry.mjs

      - name: Build trust index
        run: node registry/scripts/build-trust.mjs

      - name: Build dependency graph
        run: node registry/scripts/build-dependencies.mjs

      - name: Check for changes
        id: diff
        run: |
          git diff --quiet registry/ && echo "changed=false" >> "$GITHUB_OUTPUT" || echo "changed=true" >> "$GITHUB_OUTPUT"

      - name: Commit updated registry
        if: steps.diff.outputs.changed == 'true'
        run: |
          git config user.name "repomesh-bot"
          git config user.email "repomesh-bot@users.noreply.github.com"
          git add registry/
          git commit -m "registry: update index, trust, and dependency graph"
          git push
