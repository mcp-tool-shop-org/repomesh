{
  "name": "repomesh-ledger",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate:ledger": "node scripts/validate-ledger.mjs",
    "verify:release": "node scripts/verify-release.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1"
  }
}
